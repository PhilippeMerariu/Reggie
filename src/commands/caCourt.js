module.exports = {
    court: (channel, modifiers) => {
        channel.send('notch bitch');
    },
    caMarche: (msg) => {
        return (message === "ca marche pas" || message === "ça marche pas")
    },
    name: 'notch',
    type: 'passive'
}